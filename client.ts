import { stats } from "@/lib/data"; import { Card } from "@/components/ui"; import { MiniLine } from "@/components/charts";
const colors:Record<string,string>={purple:"from-[#6b3df0] to-[#3815b5]",green:"from-emerald-500 to-emerald-700",orange:"from-orange-400 to-orange-700",red:"from-rose-500 to-red-700"};
export default function StatsCards(){return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{stats.map(s=><Card key={s.title} className="min-h-[138px] overflow-hidden"><div className="flex items-center gap-4"><div className={`grid h-16 w-16 place-items-center rounded-full bg-gradient-to-br ${colors[s.color]} text-3xl text-white shadow-glow`}>{s.icon}</div><div className="flex-1"><p className="nepali text-sm font-bold text-[var(--muted)]">{s.title}</p><strong className="block text-3xl">{s.value}</strong><span className="text-sm font-bold text-emerald-600">{s.note}</span></div></div><div className="mt-2 h-9"><MiniLine color={s.color}/></div></Card>)}</div>}
